[project]
name = "genui-widgets"
version = "0.1.0"
description = "GenUI Widgets — ADK + A2UI starter with 10 composed rich widgets and a preview harness."
readme = "README.md"
authors = [{ name = "Sanchit Alekh" }]
license = { text = "Apache-2.0" }
dependencies = [
    "pydantic>=2.10.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "google-adk[gcp]>=2.0.0,<3.0.0",
    "a2ui-agent-sdk>=0.2.4",
    "google-cloud-aiplatform[agent-engines,evaluation]>=1.156.0",
    "google-cloud-logging>=3.12.0,<4.0.0",
    "opentelemetry-instrumentation-google-genai>=0.1.0,<1.0.0",
    "mcp>=1.0.0",
    "protobuf>=6.31.1,<7.0.0",
    "gcsfs>=2024.11.0",
    "python-dotenv>=1.0.0",
]
requires-python = ">=3.11,<3.14"

[dependency-groups]
dev = [
    "pytest>=9.0.2,<10.0.0",
    "pytest-asyncio>=1.0.0,<2.0.0",
    "nest-asyncio>=1.6.0,<2.0.0",
    "httpx>=0.27",
]

[project.optional-dependencies]
eval = [
    "google-adk[eval]>=2.0.0,<3.0.0",
    "google-cloud-aiplatform[evaluation]>=1.156.0",
]
lint = [
    "ruff>=0.4.6,<1.0.0",
    "ty>=0.0.1a0",
    "codespell>=2.2.0,<3.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle
    "F",   # pyflakes
    "W",   # pycodestyle warnings
    "I",   # isort
    "C",   # flake8-comprehensions
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
    "RUF", # ruff specific rules
]
ignore = ["E501", "C901", "B006"]

[tool.ruff.lint.isort]
known-first-party = ["app"]

[tool.ty]

[tool.ty.environment]
python-version = "3.11"

[tool.ty.src]
exclude = [".venv/**"]

[tool.ty.rules]
unresolved-import = "ignore"
unresolved-attribute = "ignore"
invalid-argument-type = "ignore"
invalid-assignment = "ignore"
invalid-return-type = "ignore"
possibly-missing-attribute = "ignore"
not-subscriptable = "ignore"
deprecated = "ignore"

[tool.codespell]
ignore-words-list = "rouge"
skip = "uv.lock,.venv,**/package-lock.json"

[tool.pytest.ini_options]
pythonpath = "."
asyncio_default_fixture_loop_scope = "function"

[tool.agents-cli]
acli_version = "0.1.3"
agent_directory = "app"
region = "us-central1"
base_template = "adk"
language = "python"
deployment_target = "agent_runtime"

[tool.agents-cli.create_params]
deployment_target = "agent_runtime"
cicd_runner = "skip"
include_data_ingestion = false
datastore = "none"
agent_guidance_filename = "GEMINI.md"
